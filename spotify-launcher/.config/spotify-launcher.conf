[spotify]
# Spotify's CEF build crashes on startup under X11/XWayland on this machine
# (SIGTRAP inside libcef). Running it as a native Wayland client avoids it.
extra_arguments = ["--enable-features=UseOzonePlatform", "--ozone-platform=wayland"]
