[ids]
*

[main]
# Caps Lock is never Caps Lock -- it is just Escape.
# Umlauts come from the layout instead: AltGr + a/e/o/u/s
# (see ~/.config/xkb/symbols/custom, selected in hypr/input.conf).
capslock = esc
